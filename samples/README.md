Samples of integrating the WProofreader plugin for Tiptap
===================================

To work with the samples, you need to install their dependencies from the root folder:
```console
npm install --prefix samples/react
```
